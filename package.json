{
  "name": "ganzu-monorepo",
  "workspaces": [
    "pkgs/*"
  ],
  "devDependencies": {
    "turbo": "^2.4.4"
  },
  "packageManager": "npm@10.9.2",
  "scripts": {
    "test": "turbo run test",
    "build": "turbo run build"
  }
}
